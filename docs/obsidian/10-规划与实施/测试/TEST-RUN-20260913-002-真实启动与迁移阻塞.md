---
id: TEST-RUN-20260913-002
type: test-record
status: startup-recovered
created: 2026-09-13
---

# 真实启动与迁移阻塞

## 最新结果：启动已恢复

用户批准修复后，V53 在删除旧组前迁移 quota_policy.group_id，保留全部配额数值；目标组已有配额时保留旧组，避免覆盖。仅备份并移除了测试库中 1 条 V53 success=0 标记，备份为 `output/admin-real-test/failed-history-before-repair.sql`。

- V53、V54 均 success=1。原配额完整转移至 User：1073741824 / 20971520 / 10 / 10000 / 1000000 / 1000 / 3；用户数仍为 21。
- 随后发现全文搜索构造器依赖未注册的 EmbeddingStore Bean，改为复用现有 QdrantVectorStoreService。新增 AdminSearchWiringTest 验证 Spring 装配。
- Maven package 成功；AdminSearchWiringTest、AdminTaskServiceTest、AdminMetricsServiceTest 共 7 项通过。
- `ylcloud-admin-test` 已 healthy，8080 启动成功；后端和前端代理 `/api/site/public-settings` 均 HTTP 200。
- 未登录请求 `/api/admin/tasks` 返回业务 code 401（请先登录），不再是代理 500。
- Docker Desktop 继续使用 Win10 身份；正式库、原有账号密码未修改。

### 隔离环境限制

测试库历史文档引用的向量不在新建隔离集合中。启动一致性扫描发现 documentId=4、16、18 的向量计数 expected=1 / actual=0，并执行隔离标记。不得将这项记录视为真实 RAG 验收通过，也不能直接认定为生产向量丢失；测试库历史状态可从迁移前备份追溯。后续 RAG 实测需准备成套隔离的 MySQL/MinIO/Qdrant 数据。

以下为首次失败时的历史记录。

## 环境及执行结果

- Docker Desktop 进程所有者经检查为 `DESKTOP-VMOCLJT\Win10`，所有 Docker 操作均在 Win10 宿主身份执行。
- 前端 Vite 已启动于 `http://127.0.0.1:5173`，Playwright 真实浏览器打开 `/login`，用户名、密码、登录与注册入口均可见。
- MySQL、MinIO、RabbitMQ、Qdrant、模型服务和解析服务已启动并报告 healthy。
- 原 `ylcloud-app` 连接正式库 `ylcloud`，保持停止，没有启动或重建。
- 已在迁移前备份 `ylcloud_space_test`，本地备份为 `output/admin-real-test/testdb-before-migration.sql`，大小 6055274 字节。该目录已加入 Git 忽略，禁止提交备份内容。
- 最新代码 Maven package 成功；通过只读挂载最新 JAR 启动独立容器 `ylcloud-admin-test`，监听 8080，连接测试库。对象桶和向量集合分别隔离为 `ylcloud-admin-test`、`ylcloud_admin_test`；异步消费者及 LLM 调用关闭。

## 实际失败

Flyway V53 在第 68 行执行：

```sql
DELETE FROM permission_group WHERE group_name = '默认用户组';
```

返回 MySQL 1451 / SQLSTATE 23000：`quota_policy.fk_quota_policy_group` 仍引用该用户组，拒绝删除。

只读检查确认该组存在 1 条配额记录；V53 当前只迁移用户分组、移除旧权限，未迁移配额。相关外键来自 `permission_group_grant`、`user_permission_group` 和 `quota_policy`。

测试库迁移记录：V53 success=0，V52/V51/V50 success=1；V54 尚未执行。正式库只读检查仍为 V52。

## 当前边界

- 为防止反复失败，已停止 `ylcloud-admin-test`；前端和基础服务保留运行。
- 没有修改既有账号密码，没有创建临时测试账号，没有执行登录后管理页业务读写测试。
- 浏览器公共设置请求因后端未启动出现错误，不等同于管理页验收通过。
- 未关闭外键、删除配额、修改迁移文件或清理 Flyway 失败记录。

## 建议后续

修复 V53 的配额迁移顺序，明确保留旧组配额并迁移到新 User 组，不以直接删除配额绕过约束；在测试库恢复可迁移状态后重新执行 V53/V54。该项涉及迁移代码及失败历史修复，等待用户确认后继续，正式库不执行。
