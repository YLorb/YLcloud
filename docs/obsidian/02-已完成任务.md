---
title: 已完成任务
type: task-status
status: maintained
updated: 2026-07-17
tags:
  - ylcloud
  - tasks
  - completed
  - testing
---

# 已完成任务

本页只记录已经实现且至少有自动化测试、构建、真实 API 或 E2E 证据的能力。仅完成代码但尚缺目标环境验证的项目归入“待验收任务”。

## 基础平台

- [x] 注册、登录、BCrypt 密码存储、JWT 鉴权与当前用户接口。
- [x] 首用户管理员串行化，避免并发注册产生多个首管理员。
- [x] 业务异常统一为 400、401、403、404、409，未知异常为 500。
- [x] 平台设置、公开站点设置、管理员权限和配置审计基础能力。
- [x] JWT、LLM、Ark、RAG Query、VLM 等敏感值改用文件型 Secret 引用。

## 文件与 Space

- [x] 个人网盘目录树、普通上传、秒传、下载、预览、重命名、移动、复制、回收站和公开分享。
- [x] 大文件分片初始化、Web Worker 哈希、断点续传、租约、合并幂等和最终 MD5/SHA1/SHA256 校验。
- [x] 分片故障注入覆盖缺片、错误摘要、八路并发合并、应用进程退出恢复和元数据事务回滚。
- [x] MinIO 版本对象彻底清理、物理文件清理 outbox、重启恢复和引用归零后清理。
- [x] Space 创建、成员管理、OWNER/ADMIN/MEMBER 权限矩阵、文件上传/导入和 Web 链接导入。
- [x] 文件版本 v1 初始化、后续版本、恢复为新版本及恢复为独立副本。
- [x] 存储配额查询和前端存储卡片。

## RAG 知识库

- [x] 文档结构化解析、质量评估、OCR/VLM 降级接口和解析结果缓存。
- [x] 结构感知切分，拒绝空正文、空切片、空白切片和 metadata fallback 伪成功。
- [x] 真实 embedding readiness、向量数量/维度/有限值校验和 Qdrant 精确 point count 校验。
- [x] 文档状态与向量状态解耦，RAG 成功要求 document、active chunk ref 和 Qdrant point 一致。
- [x] 向量、BM25、关键词、Metadata 多路召回，候选融合、邻接扩展和 Rerank。
- [x] Query Rewrite、Multi Query、HyDE、StepBack 规划及最终回答生成。
- [x] 引用包含 Space、文件、文档和预览/下载定位信息。
- [x] no-answer 使用显式语义标志，统一清空 citations、contexts、hitChunkIds 与查询日志命中 ID；模型不可用时保留可人工核验的引用。
- [x] RAG 配置、重建、失败重试、向量修复、查询日志和分析接口。
- [x] 启动与定时对账，异常文档隔离并通过 CLEANUP_PENDING 状态补偿。
- [x] MySQL 与 Qdrant 跨存储一致性、CAS 提交、活动任务唯一约束和超时回收。
- [x] 增加 PPTX 逐页结构化解析，提取标题、正文、表格与备注；metadata fallback 不再冒充正文成功，解析器版本提升为 structured-v3。
- [x] parent 与唯一 child 内容相同时按内容哈希去重，修复 metadata 被重复保存成两个 chunk 的问题。

## 知识画像与知识会话

- [x] 每个知识库的知识画像开关，默认 true，OWNER/ADMIN 可手动关闭或开启。
- [x] RAG 完成后读取最新开关；关闭时记录明确跳过状态，不生成画像版本或事件。
- [x] 画像批次独立统计成功/失败文档数，前端可恢复显示完成通知。
- [x] 画像生成、JSON 解析、标准化、质量校验、自动修复和 NEEDS_REVIEW 流程。
- [x] 来源快照签名、revision、增量决策和 SYNC_RETRIEVAL_SOURCE。
- [x] 分类、标签、问题、审计日志、版本差异、恢复、重新生成和重新分类。
- [x] 多知识库问答、会话列表、可恢复异步问答、消息持久化和跨 Space 权限校验。
- [x] AI Assistant 新建会话使用显式草稿态，查询刷新不再自动选回旧会话。

## 前端与部署

- [x] 网盘、Space、知识库、知识画像、AI Assistant、异步任务、设置、分析和公开分享主要页面。
- [x] 文件列表/网格多选、批量移动/复制/回收站，以及图片/视频/音乐/文档跨目录聚合查询。
- [x] Admin Settings 四分区、角色配额、用户和用户组管理、组授权与单用户权限覆盖。
- [x] 权限管理弹窗改为响应式 Grid，消除中文说明逐字竖排。
- [x] 后台任务增加归属校验的详情 API、成功/失败摘要、阶段/耗时/统计、错误脱敏和可点击详情弹窗。
- [x] 在 `frontend-ui-ux` 分支完成新前端壳层与页面替换：React Router 受保护路由、TanStack Query 服务端状态、Radix 无障碍组件、语义设计变量、明暗主题和路由拆包。
- [x] 成功/确认统一为绿色，危险/失败/系统错误统一为红色；状态同时使用图标与文本，不只依赖颜色。
- [x] 删除旧手写路由、旧侧栏、旧知识库重复页面及约 125 KB 旧样式；桌面侧栏收起和 390px 移动端布局通过真实 Edge/Playwright 检查。
- [x] 前端 Vitest 4 个测试文件、10 项断言通过；TypeScript 严格生产构建通过。
- [x] 2026-07-16 使用 Win10 本机 Docker Desktop 身份重建 `frontend`、`ylcloud-app`、`model-service`、`document-parser-service` 四个镜像，七个 Compose 服务全部健康。
- [x] 2026-07-17 使用 Win10 本机 Docker Desktop 身份重新构建并替换 `ylcloud-app` 与 `frontend`；Flyway 26 项迁移校验通过、schema 版本 25，七服务健康且 `/assistant` 与 API 代理返回 200。
- [x] 新镜像真实 API E2E 通过：普通文件与 Space 文件上传下载哈希一致，Markdown 解析得到 2 个 chunk，BGE/Qdrant 索引和查询命中 2 个 chunk，PDF 解析索引成功，版本恢复哈希一致。
- [x] 修复 `FlagEmbedding 1.4.0` 与 `transformers 5.13.1` 的 rerank 运行时不兼容：固定兼容版 `transformers 4.57.6`，真实 BGE rerank 热请求 2.47 秒完成且相关文档排序第一。
- [x] Docker Compose 七服务空卷启动和健康依赖。
- [x] Docker Hub 镜像编排和四个自建镜像发布工作流。
- [x] 本地/服务器统一自部署脚本：补齐配置、迁移 Secret、预检、构建或拉取、健康等待、V22 校验、失败诊断。
- [x] 当前后端全量测试 121 项通过；前端生产构建、模型服务专项和多轮真实 E2E 已有通过记录。
- [x] E2E smoke 改为复用受控固定账号，并在运行前清理遗留测试 Space；自部署脚本不再允许无密码启动 smoke。

## 文档治理

- [x] 按项目定位、任务状态、架构、状态机、功能导航、功能详解和问题收集重建 Obsidian 权威结构。
- [x] 55 篇 Markdown 全部使用 YAML frontmatter 和领域标签；导航链接按“总览 → 分区 → 细分文档”单向组织。
- [x] 旧 Vault 内容完整移动到日期归档，不再与当前状态页竞争权威性。
