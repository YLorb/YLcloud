---
id: TEST-20260813-001
type: test-requirement
status: pending-verification
priority: P0
created: 2026-08-13
updated: 2026-08-14
owner: unassigned
version: 3
tags: [sandbox, testing, security]
---

# Sandbox 实施测试要求

必测：严格契约；未知 Tool/版本；参数 Schema；幂等冲突；Workflow `sandbox` 分支；无 Client、取消、超时、安全错误、输出 Schema 与 Trace 子 Span 脱敏；固定镜像/入口且不经 Shell；非 root、只读根、无网络、cap drop、no-new-privileges、seccomp、CPU/内存/PID/超时；路径穿越、符号链接、文件大小/数量和 zip bomb；服务停止不回退。

单元、回归、异常与安全测试必须全部通过。Docker/Linux 门禁未执行时只能标记 `pending-verification`。实现 Agent 必须补齐每个用例的前置条件、数据、步骤、预期、实际、证据与结论。

## 2026-08-13 实际结果

| 范围 | 命令/环境 | 实际结果 | 结论 |
|---|---|---|---|
| Sandbox 单元/API/安全命令 | Python 3.12 + pytest | 10 passed | 通过 |
| Workflow 全量回归 | Python 3.12 + pytest | 302 passed, 7 skipped | 通过；跳过项为外部集成条件 |
| Python 静态编译 | `compileall` | PASS | 通过 |
| 主 Compose 与独立 Runner Compose | `docker compose config --quiet` | PASS | 通过 |
| Win10 真容器全链路 | `DESKTOP-VMOCLJT\\Win10`、Docker Desktop 4.82.0、Engine 29.6.1 | 固定 image ID；API 调用 `SUCCEEDED`；幂等 Span 相同；主体历史 1 条 | 通过 |
| Win10 容器安全配置 | Docker inspect | `network=none`、只读根、UID 65532、`CapDrop=ALL`、`no-new-privileges`、PID 64、CPU 1、内存/Swap 512 MiB | 通过 |
| 生产 Linux Rootless 门禁 | 独立 Runner 节点 | 未执行 | 待验证 |

已验证：固定 digest、输入 Schema 前置拒绝、幂等重放/冲突、服务令牌、主体范围历史、超时安全状态、无 Shell 的 Docker 参数、Workflow 远端分支、无 Client 不降级，以及成功/失败/超时 Sandbox 子 Span 脱敏后关联父 Trace。

Win10 已验证真实容器默认断网、非 root、只读根以及 Docker HostConfig 的 cgroup/PID/capability/安全选项。仍未验证：生产 Linux Rootless Socket 权限、AppArmor/SELinux、超时后完整进程树与孤儿容器回收、恶意文件/zip bomb、MinIO 输入输出搬运和 RabbitMQ 租约恢复。以上完成前整体状态不得改为 `completed`。
