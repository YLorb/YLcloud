---
id: TEST-20260817-102
type: test-requirement
status: pending-verification
priority: P0
created: 2026-08-17
updated: 2026-08-17
owner: unassigned
version: 1
tags: [space, file-tree, search, deduplication, testing]
---

# Space 目录操作、搜索与内容防重测试要求

## 必选范围

1. 0/1/99/100/101 层创建和移动，历史超深读取。
2. 重命名/移动后的全部后代 path/depth/version，陈旧版本和事务回滚。
3. 自身/后代移动环、跨 Space、非目录、已隔离目标和并发操作。
4. 全 Space 搜索分页、定位路径、特殊字符、空查询和权限/隔离过滤。
5. 相同 UUID、相同 SHA-256 不同 UUID、并发上传/导入和删除中重复。
6. 历史重复报告完整且零数据修改。

## 通过标准与记录

- 无目录环、超过 100 层、新增重复有效内容或越权搜索结果。
- 实现 Agent 创建独立执行记录并回链 TASK-20260817-002，附真实 MySQL 证据。

## 执行记录（2026-08-17）

- 编译及后端回归 310/310 通过；前端 production build 通过。
- 静态核对通过：CAS 条件、100 层上限、环检测、ACTIVE 搜索过滤、`(space_id,content_hash)` guard、历史重复只报告。
- 未执行：真实 MySQL Flyway、递归 CTE、并发 guard、100 层和大 Space 性能；Docker 引擎未运行。恢复后必须补齐，搜索 cursor 也须在性能门禁前实现。
