---
id: TEST-RUN-20260913-001
type: test-record
status: partial
created: 2026-09-13
updated: 2026-09-13
tags: [admin, api, verification]
---

# 管理后台第一批实现验证

## 范围与状态

分支：`feat/adminsetting`。本记录不是整套 REQ-20260913-002 的完成证明。

已写入代码：

- 用户真实列表、创建、角色/状态、分组与权限覆盖；权限失败隔离、密码关闭即清除、拒绝后端失败时乐观更新。
- 用户组权限 CRUD 与独立配额读写；系统组/被引用组禁止界面删除。
- 后端动态配置键读写与主机维护；敏感值不回填，确认才发命令。
- 已索引 Space 文件全文搜索，服务端分页、候选上限说明、纯文本 Chunk 证据。
- `/api/admin/tasks` 全站分页查询、`/{id}/archive` 终态归档，复用原重试/取消端点。归档保留任务及关联执行证据，排除 TTL 删除和重新重试。
- `/api/admin/metrics/daily` 上海自然日统计，分离引用/Blob/用户/Space，Token 显式不可用。
- V54 迁移新增任务归档时间、操作人及索引。迁移尚未应用到任何数据库。

## 前置与数据

- 本地已安装 Maven/npm 依赖。
- 前端 API 使用 Vitest 替身，不连接真实账号或执行维护。
- 后端使用 JUnit/Mockito；没有运行 Flyway、MySQL、Qdrant 或从机集成。
- 测试用户名/密码均为本地固定虚构值，不写入正式账号库。

## 用例与预期

| 用例组 | 操作与预期 | 证据 |
|---|---|---|
| 用户读写 | 服务器列表、创建回包、错误留表单、清除密码、权限覆盖及解除分组；辅助接口失败不遮蔽列表 | `cloud-frontend/src/features/admin/LiveUsersPage.test.tsx`，7 项 |
| 管理页 | 配额保持全部原值、保护被引用组、密钥不回填且未改不提交、维护确认、服务器任务分页归档、首页 Token 不伪造、检索证据纯文本 | `cloud-frontend/src/features/admin/LiveAdminPages.test.tsx`，7 项 |
| 原型及其他前端回归 | 原有界面和非管理功能无回归 | `npm run test` |
| 任务服务 | 页码上限、长整型偏移、筛选传参、仅成功条件更新后审计、状态冲突拒绝 | `AdminTaskServiceTest`，4 项 |
| 权限边界 | 非 ADMIN 在数据查询及归档前拒绝 | `AdminTaskControllerTest`、`AdminMetricsControllerTest`，各 1 项 |
| 指标服务 | 日期范围限制、上海时区边界、缺失日期补零、引用/Blob 分离 | `AdminMetricsServiceTest`，2 项 |
| 任务状态机回归 | 原状态流与 outbox 等行为保持 | `UnifiedTaskCenterServiceTest`，8 项 |

## 执行记录

1. `npm run build`：最终构建通过，1855 个模块，退出码 0。过程中修正了测试替身返回数组与 `void` 接口不一致的类型错误。
2. `npm run test`：最终完整回归 9 个文件、57 项全部通过。首次唯一失败是新增测试用文本匹配“新增 Blob”同时命中卡片和表头，修正为 columnheader 角色定位后通过。
3. `mvn -pl cloud-server -am test -Dtest=AdminTaskServiceTest,AdminTaskControllerTest,UnifiedTaskCenterServiceTest,AdminMetricsServiceTest,AdminMetricsControllerTest -Dsurefire.failIfNoSpecifiedTests=false`：16 项通过、0 失败、0 跳过，BUILD SUCCESS。
4. `git diff --check`：无空白错误；存在工作区既有 CRLF 转换提示。
5. 前端早期测试首次因沙箱拒绝写入 Vitest 缓存而进程退出非零；获得执行权限后重跑正常。该情况未计为业务测试通过。

## 未完成与发布限制

- 未接入：备份/恢复验证、安全审计、通用系统事件、全站文件引用治理、管理员免资源授权、节点绑定/复检，以及持久检查点/人工续跑。
- 其余未适配路由仍是明确标注的原型；能力级 unavailable 切换尚未全部完成，不能视为生产发布就绪。
- 设置页面尚需补充站内离开脏表单保护；所有新增真实页面尚需浏览器响应式、键盘和后端错误联调。
- 任务详情、归档数据库并发/TTL 实测及指标 MySQL 时区契约测试尚未完成。
- 暂缓项仍为存储策略完善及 Token 计量，不在本批实施。
- 未修改账号密码，未操作正式数据，未部署、提交 Git 或同步分支。
- 上线前必须在测试库验证 V54、角色矩阵、任务归档与分页及指标 SQL，并完成剩余 TASK；本批状态保持 in-progress。
