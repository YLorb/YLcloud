---
id: TASK-20260913-009
type: implementation-task
status: in-progress
priority: P1
created: 2026-09-13
updated: 2026-09-13
owner: unassigned
version: 1
tags: [admin, frontend, datasource]
---

# 管理页能力级数据源

- 目标：将 `AdminDataSource` 的纯 `mock` 类型改为按领域区分 `api/mock/unavailable` 的查询与命令接口，为后续页面接入提供统一来源、错误、分页和能力标识。
- 前置与输入：无；输入为 `features/admin/core/AdminDataSource.tsx`、`AdminCollectionPage.tsx`、现有 `api.ts` 及 14 路由。
- 产物：按领域适配器接口、能力清单、真实/Mock/不可用提示、服务端分页参数与请求取消机制；保留可注入 Mock 测试源。
- 不包含：任何领域业务 API 或假装真实的泛化 CRUD。
- 验收：真实失败不变空态，真实命令不出现“演示成功”；未支持命令隐藏/禁用；无越权网络回退；旧 Mock 测试仍可控。
- 测试：类型/组件单元、来源标识、loading/empty/error、重复提交、竞态、14 路由回归、安全测试；全部通过才算完成。
- 风险与回滚：通用组件硬编码演示措辞易漏，逐路由验；可回滚前端数据源适配，不删后端数据。
- 回写：实现 Agent 生成完整用例和独立执行记录，按证据更新本任务状态；下游 [[10-规划与实施/测试/TEST-20260913-002-管理后台真实API与节点绑定测试要求]]。
