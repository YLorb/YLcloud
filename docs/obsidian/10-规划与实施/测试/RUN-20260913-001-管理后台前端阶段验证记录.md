---
id: RUN-20260913-001
type: test-record
status: pending-verification
created: 2026-09-13
updated: 2026-09-13
tags: [frontend, admin, mock, testing]
---

# 管理后台前端阶段验证记录

## 范围

本记录只针对 `/admin` 前端 Mock 原型，不代表后端管理 API、真实权限、支付、备份、OAuth 或审计能力已通过测试。测试需求见 [[10-规划与实施/测试/TEST-20260913-001-管理后台前端页面验收要求]]。

## 已执行

| 检查 | 结果 | 证据 |
|---|---|---|
| 14 个页面组件的标题、演示标识和默认空态 | 通过 | `src/features/admin/AdminPages.test.tsx` 的参数化测试 |
| Mock 默认空、创建、更新、删除、实例间不持久化 | 通过 | 同上 |
| 存储策略创建、启用二次确认、备份历史标签 | 通过 | 同上 |
| 事件载荷作为文本转义显示 | 通过 | 同上 |
| 25 条用户 fixture 的分页和搜索 | 通过 | 同上 |
| 数据源失败时的错误态和重试入口 | 通过 | 同上 |
| 分享撤销只改变状态、不删除记录 | 通过 | 同上 |
| 失败任务重试的二次确认与状态变更 | 通过 | `src/features/admin/AdminPages.test.tsx` |
| 参数设置脏状态与放弃修改确认 | 通过 | `src/features/admin/AdminPages.test.tsx` |
| OAuth 创建后一次性显示无效演示 Secret | 通过 | `src/features/admin/AdminPages.test.tsx` |
| 编辑用户组保留成员统计、订单类型/日期组合筛选 | 通过 | `src/features/admin/AdminPages.test.tsx` |
| 后台任务详情演示时间线 | 通过 | `src/features/admin/AdminPages.test.tsx` |
| `npm run test` | 通过 | 7 个测试文件、43 个测试用例通过 |
| `npm run build` | 通过 | TypeScript 编译和 Vite 生产构建通过 |
| 浏览器：存储策略桌面空态、移动端表单提交 | 通过 | Playwright CLI，1280×720、390×844、320×700；提交后列表和统计更新 |
| 浏览器：未登录直接访问 `/admin/dashboard` | 通过 | 重定向至 `/login` |
| 浏览器：320/390px 整页横向溢出 | 通过 | `documentElement.scrollWidth === innerWidth` |

## 仍需补验

- 本轮未覆盖 14 个页面各自的有数据/错误 fixture、完整键盘流程和四种规定宽度的全部页面截图。
- 未验证真实管理员登录后的全部路由和生产后端数据；本机后端未启动，真实登录页的 `/api/site/public-settings` 请求返回 500。
- 订单、节点、任务、举报和 OAuth 等页面目前是前端交互原型，相关真实业务动作、权限边界和协议契约必须在后端接入后另行验收。
- 测试要求中的跨页面状态、深色主题、复杂边界数据和全部浏览器 E2E 门禁尚未完成，因此本记录保持 `pending-verification`。
