---
id: TASK-20260725-017
type: implementation-task
status: completed
priority: P0
created: 2026-07-25
updated: 2026-07-27
owner: codex
version: 3
tags: [mq-v2, cleanup, maintenance]
---

# mq-v2：清理与维护任务迁移

## 目标

将物理文件清理和维护/一致性修复中的重型执行迁入 cleanup、maintenance 队列，先验证低并发、强幂等任务的完整生产语义。

## 依赖与输入

- TASK-20260725-016 已发布并稳定。
- TASK-20260725-004 已定义 Space 文件/知识生命周期、引用账本、幂等键和资源版本；本任务迁移其清理与跨存储维护执行，不得重定义领域状态。
- `PhysicalFileCleanupExecutor`、`MultipartUploadCleanupService`、`CrossStoreRecoveryService`、`RagIndexConsistencyService` 等现有实现。

## 修改范围与产物

- 为物理文件、过期分片、跨存储恢复、索引一致性修复定义稳定 `taskType`、`task_key`、`resource_key/version`。
- 定时扫描器仅创建统一任务；消费者调用拆出的同步领域执行函数。
- MinIO/数据库/Qdrant 清理变为幂等：不存在视为成功，删除使用资源版本栅栏。
- 增加关联 `async_task_id`、失败码、指标和详情链接。
- Feature Flag：`async.mq.cleanup`、`async.mq.maintenance`，按域独立开关。
- 默认并发各 1、prefetch 1；压测后才能上调。

## 不包含

- Memory、Chat、Pipeline、RAG 主索引迁移。
- 删除定时扫描器；只改变其职责。

## 验收标准

1. 同一待清理对象重复扫描只生成一个有效任务。
2. 重复投递/Worker 崩溃不会误删新版本资源。
3. 4 次失败进入 `FAILED`，详情展示每次原因；授权用户可复用 taskId 人工重试。
4. 关闭任一域 Flag 可恢复旧执行路径，且不会同时双跑。
5. Rabbit 停机期间扫描产生 `PENDING_PUBLISH`，恢复后完成。

## 测试与通过

- 覆盖对象已不存在、网络超时、部分删除、资源已更新、取消、租约过期和重复消息。
- 验证真实 MinIO/Qdrant/MySQL 集成或等价 Testcontainers。
- 满足 TEST-20260725-002 的通用门禁与 `mq-v2` 场景。

## 风险与回滚

- 清理副作用不可逆：每次副作用前重读资源版本和引用计数。
- 回滚时关闭对应 Flag；已创建统一任务暂停/取消，不回放到旧执行器。

## Git 与回写

- 推荐提交：`feat(async): migrate cleanup and maintenance workers (mq-v2)`。
- 独立测试、commit、push；记录迁移任务类型、开关、指标和故障演练。
- 下一任务：[[10-规划与实施/任务/TASK-20260725-018-mq-v3用户记忆任务迁移]]
- 测试：[[10-规划与实施/测试/TEST-20260725-002-统一异步任务与消息队列测试要求]]

## 实施回写

- 新增 `cleanup`、`maintenance` 独立迁移开关以及两条默认单并发、prefetch 1 的消费队列；关闭开关时保留原执行路径，已有活动统一任务会阻止旧路径并行执行。
- 统一任务中心新增通用领域任务创建入口，以稳定 `task_key`、仅含资源 ID 的 payload、`resource_key/resource_version` 和事务 outbox 创建任务；重复扫描命中唯一键并复用同一任务。
- 清理域迁移 `PHYSICAL_FILE_CLEANUP`、`MULTIPART_EXPIRED_CLEANUP`、`MULTIPART_MERGED_CLEANUP`；维护域迁移 `CROSS_STORE_RECOVERY`、`RAG_CONSISTENCY_CLEANUP`、`RAG_CONSISTENCY_VALIDATE`。
- Flyway V43 为物理文件、分片上传、跨存储操作和 RAG 文档增加统一任务关联与资源版本栅栏。消费者执行前重读关联、状态、版本及引用，副作用前后执行取消/租约检查；迟到结果以 `SKIPPED_STALE` 收敛。
- 定时器只扫描和登记统一任务，领域执行函数保持同步且幂等；对象已不存在视为成功，MinIO/Qdrant 暂时失败进入统一重试，跨存储补偿结果保留原领域状态证据。
- RabbitMQ 停机期间 outbox 保持 `PENDING`、任务保持 `PENDING_PUBLISH`；Broker 恢复后自动续发并由 inbox 去重消费。

## 验收结果

- TASK 专项与关联测试 23 个、后端全量 252 个、前端 12 个全部通过；Spring Boot 与 Vite 生产构建成功。
- Win10 Docker 真实执行物理文件清理和跨存储恢复均成功，cleanup/maintenance 消费者各 1，队列最终无 ready/unacked 消息。
- RabbitMQ 停机演练中任务保持 `PENDING_PUBLISH`；恢复后同一任务以 attempt 1 成功，outbox 状态为 `SENT`，inbox 状态为 `PROCESSED`。
- 已有 V42 验收库成功前滚 V43；重启校验 44 个 Flyway 迁移，应用 `/actuator/health` 返回 `UP`，MySQL、RabbitMQ、MinIO、Qdrant 均为 `healthy`。
- 详细记录：[[10-规划与实施/测试/TEST-20260725-013-TASK-017-清理与维护任务迁移测试记录]]。
