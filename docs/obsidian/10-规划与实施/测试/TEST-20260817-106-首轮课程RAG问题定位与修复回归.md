---
id: TEST-20260817-106
type: regression-report
status: partially-verified
priority: P0
created: 2026-08-17
updated: 2026-08-17
tags: [rag, course-testset, upload, retrieval, sandbox, space-dissolution, docker]
---

# 首轮课程 RAG 问题定位与修复回归

## 1. 结论

首轮报告揭示了四个独立问题，不能笼统归因为“RAG 不准”：

1. 上传入口的产品上限、Spring multipart 上限和账号配额是三层不同限制；其中 Spring 20 MiB 配置错误已修复。
2. Q04 的 `0 context` 不能直接证明没有召回，因为旧响应会在生成器判定 no-answer 后清空检索证据；同时确实存在候选排序截断缺陷。两者均已修复并用真实 PDF 验证。
3. 约 110 秒延迟主要发生在查询改写和多路检索，不是最终答案生成。并行化后真实 Q04 从 131.4 秒降至 69.55 秒，但仍属 P1 性能问题。
4. TEAM Space 解散只有 outbox 生产者、没有消费者，导致永久停留在 `DISSOLVING/PENDING`。消费者和幂等清理流程已补齐，历史 Space 16 与新建回归 Space 均自动收敛。

本轮没有把 Q05–Q19 计入准确率；它们必须在不会重建后端容器的独占窗口重新执行。

## 2. 问题、根因与解决方案

| 优先级 | 现象 | 根因定位 | 已实施方案 | 验证状态 |
|---|---|---|---|---|
| P0 | 20 MiB 以上上传返回 HTTP 500 | `spring.servlet.multipart` 仍为 20/25 MiB，与站点 2 GiB 宣称和业务上传限制不一致；异常未映射 | multipart 与业务限制统一读取 `YLCLOUD_UPLOAD_MAX_*`；超限映射 HTTP/业务码 413 | 单元测试通过；21.47 MiB 文件已越过 Spring 层并进入账号配额校验 |
| P0 | Q04 返回 0 context | no-answer 分支清空 `hitChunkIds/contexts/citations`，使“无检索结果”和“检索后生成拒答”不可区分 | 新增 `retrievedChunkIds`，无论是否拒答都保留检索证据；保留原有最终引用语义 | 真实 Q04 返回 retrieved=4、hit=4、citation=4 |
| P0 | Q04 标准化石未命中 | 融合候选按相关度产生后，又按文件位置排序，再以 candidateTopK 截断；文档后部的高相关 chunk 会被丢弃 | `expandContextChunks` 保留融合相关度的 `LinkedHashMap` 顺序，不再按物理位置重排 | 单元测试覆盖“相关度顺序优先”；真实答案覆盖全部五个要求点 |
| P1 | 有效问答平均约 110 秒 | 查询改写、Multi-query、HyDE、Step-back 及多个向量路由串行；缺少阶段耗时证据 | 三类扩展改写并发，独立向量路由并发；响应和日志增加 rewrite/retrieval/generation/total 耗时 | Q04 131.4 秒 → 69.55 秒；仍需继续优化 |
| P0 | 测试被容器重建中断后混入准确率 | 脚本只看到连接失败，不能证明运行环境是否已变化 | 固定预期 `ylcloud-app` 容器 ID；每次 API/上传前后校验；区分环境变化、传输失败和业务失败；保留 resume | PowerShell 语法检查通过；待完整独占 E2E 验证 |
| P0 | Space 16 为 `DISSOLVING`，outbox 为 `PENDING` | 只有事件写入，没有调度消费者 | 新增带租约、失败退避、过期任务恢复的消费者；先严格删除 Qdrant 向量，再事务清理逻辑引用、文档、节点、成员并标记 `DISSOLVED` | Space 16：`DISSOLVED/SUCCEEDED`；回归 Space 27/28 同样成功 |
| P0 | 同一文件第二次 Sandbox 预检返回 409 | 幂等键只含用户和内容哈希，但请求摘要包含每次变化的 trace；相同键必然对应不同摘要 | 键纳入文件名、内容哈希、大小、用户；摘要排除 invocation/trace 尝试元数据 | 两次不同 invocation/trace 返回同一成功结果和 span，无 409 |
| P0 | Sandbox 工具故障被提示为“文件不安全” | 客户端把非 `SUCCEEDED` 和 `safe=false` 合并处理 | 工具执行失败返回 503；只有成功执行且 `safe=false` 才返回文件风险拒绝 | 单元测试覆盖两种语义 |
| P0/部署 | Win10 重建 Sandbox 后工具 exit 126 | Docker Desktop proxy socket 属于 GID 0，Compose 默认补充 GID 1000，Runner 无权访问 socket | Win10 回归显式设置 `YLCLOUD_SANDBOX_DOCKER_GID=0`；`.env.example` 要求按 socket 实际 GID 配置 | 容器内 `docker version` 成功，真实 PDF 预检/上传成功 |
| 已验证 | 服务 JWT 401 | 旧镜像签发的 token 缺少 `typ: JWT` | 已在先前修复中重建并验证，不重复改动 | 通过 |
| 已验证 | 临时账号与正式库安全 | 测试账号清理和管理员哈希恢复必须限定测试库 | 测试脚本保留库名、账号关系和管理员身份三重断言 | 12/13/17 为 `PURGED`；正式库未连接、未修改 |

## 3. 上传限制的正确边界

“支持 2 GiB”只表示 HTTP/对象存储入口允许该大小，不等于所有账号、所有知识化解析链路都能处理 2 GiB：

1. Spring multipart：当前默认单文件 2 GiB，请求额外保留 16 MiB envelope；超过时返回 413。
2. 账号/团队配额：仍可在 multipart 之后返回配额错误。本轮 Docker 测试账号上传 21.47 MiB 文件得到 429，证明请求已越过原 20 MiB Spring 限制；这不是 multipart 回归。
3. RAG 解析：当前 `ylcloud.rag.extraction.max-file-size` 仍为 50 MiB。不得为了匹配 2 GiB 宣传而直接把解析器内存上限提高到 2 GiB，否则会引入内存和拒绝服务风险。

后续产品语义应明确为：文件可存储至 2 GiB；知识化按格式和解析能力异步执行，超出知识化边界时文件仍可管理，但状态必须显示“未知识化/不支持”及原因。若要知识化超大文件，应实现流式/分段解析，而不是整体读入内存。

## 4. Q04 分层回归证据

测试库：`ylcloud_space_test`；文档：`第4章 地质年代与地质作用-.pdf`，12,229,777 bytes；默认知识化得到 126 chunks。

最终回答：标准化石延续时间短、演化快、分布广、数量多、特征显著，且易寻找和鉴定。该回答满足 Q04 的五个必需点。

| 指标 | 数值 |
|---|---:|
| retrieved chunks | 4 |
| final hit chunks | 4 |
| citations | 4 |
| rewrite | 28,814 ms |
| retrieval + rerank | 39,295 ms |
| generation | 1,443 ms |
| server total | 69,552 ms |
| client total | 69,673 ms |

这证明 Q04 已从“证据不可见且答案失败”修复为“召回、答案、引用均成功”。它不证明整个测试集 Recall@K 已达标，因为数据集目前只有期望文件名和答案要点，没有标注目标 chunk ID。

## 5. 性能问题仍需继续处理

本轮已经把延迟从约 110–131 秒降低至约 70 秒，但 70 秒仍不适合作为常规交互目标。阶段数据表明最终生成只占 1.4 秒，继续更换回答生成模型不会解决主要问题。

下一步方案按优先级执行：

1. 为改写、每条 embedding/Qdrant 路由、BM25/metadata 查询和 rerank 增加子阶段计时，避免把 39 秒归到一个黑盒。
2. 将多条查询 embedding 改为单次 batch `/embed`，然后执行 Qdrant 查询；不要在单 worker 模型服务上用多个 HTTP 请求制造竞争。
3. 根据 `precise/balanced/broad` 定义固定路由预算。默认 balanced 不应无条件同时执行 Multi-query、HyDE、Step-back 全部路线。
4. 对标准化后的 query plan 和 query embedding 加短期、按模型版本隔离的缓存。
5. 在独占环境至少运行 Q01–Q19 三轮，报告 P50/P95、各阶段耗时以及准确率变化，确认性能优化没有牺牲召回。

## 6. 解散消费者的一致性边界

清理顺序固定为：

1. 领取 outbox 租约；
2. 严格删除并确认 Space 的 Qdrant 向量；
3. 数据库事务内释放活动逻辑文件引用；
4. 物理引用计数归零时才提交物理删除任务；
5. 禁用 chunk 引用并清理 RAG 文档、Space 文件和成员；
6. Space 标记 `DISSOLVED`，事件标记 `SUCCEEDED`。

任何跨存储失败都会保留事件并退避重试，不使用危险 SQL 跳过状态机。重复处理 `DISSOLVED` Space 是幂等成功。

## 7. 尚未关闭的问题

| 优先级 | 问题 | 关闭条件 |
|---|---|---|
| **P0** | **RAG 检索召回不足，Q02/Q11/Q15 失败** | 扩大召回（相邻 chunk + topK=10 可配置）后回归通过 |
| P0 | Q05 正则标注与源文档不一致 | 修正测试集正则 pattern |
| P0 | Q10 源文档确实遗漏"地震作用" | 确认源文档内容，必要时修正测试集期望 |
| P1 | 首问约 70 秒仍慢 | 明确延迟 SLO；三轮 P95 达标且答案质量不下降 |
| P1 | 缺少目标 chunk 标注 | 为 QA 集补充 evidence chunk/section，计算 Recall@5、Recall@10、MRR |
| P1 | 50 MiB 知识化与 2 GiB 存储边界未在 UI 明示 | 上传前提示、文件知识状态、失败原因和重试入口全部可见 |
| P2 | Qdrant Java client 1.17 与 server 1.15.4 不兼容告警 | 客户端/服务端版本对齐并完成索引、查询、删除回归 |
| P2 | Flyway 对 MySQL 8.3 发出超出已验证版本告警 | 升级 Flyway 或锁定受支持 MySQL 版本并验证迁移 |
| P2 | Docker socket GID 依赖人工配置 | 启动脚本自动探测 socket GID，启动前 fail-fast 校验 `docker version` |

## 8. 验证范围

- Java 针对性测试：14/14 通过；全量后端测试：328/328 通过。
- Sandbox 真实幂等重放：不同 invocation/trace，结果与 span 均重放，未产生 409。
- Docker 完整服务：运行于 Win10 Docker Desktop；后端数据源明确为 `ylcloud_space_test`。
- 真实 Q04：通过，临时 Space 28 最终 `DISSOLVED`、outbox `SUCCEEDED`。
- 原始分层报告：`output/rag-course-e2e/course-rag-evaluation-20260817.md`。

## 9. 第二轮回归测试（2026-08-20）

### 测试环境

- 分支：`dev`（含 Space 解散消费者、RAG 检索修复、Sandbox 预检改进）
- 容器：Win10 Docker Desktop，9 个服务全部 healthy
- 测试集：`rag-testset/course-qa-v1.jsonl`（19 题）
- 文档：12 个课程文档（Linux 8 + 地球科学 4），全部上传并索引成功

### 测试结果

| 指标 | 数值 |
|---|---|
| 文档上传+索引 | 12/12 全部成功 |
| QA 通过 | **14/19 (74%)** |
| 失败 | 5 题 |
| 需人工复核 | 1 题（Q19 安全隔离） |

### 通过的题目 (14/19)

Q01, Q03, Q04, Q06, Q07, Q08, Q09, Q12, Q13, Q14, Q16, Q17, Q18, Q19

### 失败题目证据链

#### Q02：相对年代通常用哪些方法确定？

- **检索**：4 chunks 全部来自 `第4章 地质年代与地质作用-.pdf`，chunk ID: 2396, 2450, 2393, 2365
- **chunk 2396**：包含"地层学方法"和"古生物学方法"，**不包含构造地质学方法**
- **生成器输出**：只列出了地层学和古生物学两种方法
- **判分**：`"构造地质学": false` → 失败
- **根因**：PDF 中"构造地质学方法"在另一个 chunk 中，未进入 top-4

#### Q05：相对年代方法有什么局限？

- **检索**：4 chunks 全部来自 `第4章 地质年代与地质作用-.pdf`
- **chunk 2449/2450**：包含完整局限性内容
- **生成器输出**：完整回答了三个局限点
- **判分**：正则 `无.*化石|没有.*化石` vs 原文"尚未发现明显化石" → 不匹配
- **根因**：测试集正则 pattern 与源文档实际表述不一致，**测试集标注问题**

#### Q10：内力地质作用的主要类型有哪些？

- **检索**：4 chunks 全部来自 `第4章 地质年代与地质作用-.pdf`
- **chunk 2481**：明确列出 3 项："岩浆作用、变质作用、构造作用"，**没有地震作用**
- **生成器输出**：列出了岩浆作用、变质作用、构造作用（3项）
- **判分**：`"地震作用": false` → 失败
- **根因**：源文档在该节确实只列出了 3 种类型，**测试集期望与源文档不一致**

#### Q11：Linux 大作业使用什么虚拟化软件安装 Ubuntu 24.04？

- **检索**：`retrievedChunkIds=[815,780,821,806]`，检索到了 4 个 chunk
- **过滤后**：`hitChunkIds=[], contexts=[], citations=[]`，**全部被过滤**
- **生成器输出**："无法从当前知识库回答。知识库中仅提及使用虚拟机安装Ubuntu20.04系统"
- **判分**：`"Oracle\\s+VM\\s+VirtualBox|VirtualBox": false` → 失败
- **根因**：检索到的 chunks 被 rerank/scoreThreshold 过滤掉，**过滤阈值过严**

#### Q15：对比地质作用和 Linux 软件安装，它们分别属于哪份资料？

- **检索**：`retrievedChunkIds=[2420,2421,2375,2364]`，检索到了 4 个 chunk
- **过滤后**：`hitChunkIds=[], contexts=[], citations=[]`，**全部被过滤**
- **生成器输出**："无法从当前知识库回答。"
- **判分**：全部失败
- **根因**：跨文档查询无法同时覆盖两个文档，且 chunks 被过滤，**跨文档检索失败**

### 结论与修复计划

**核心问题**：RAG 检索召回不足，topK 过小且相邻 chunk 未被召回。

**修复方案**（最高优先级）：
1. 向量检索返回 topK 个候选 chunk 后，为每个候选 chunk 获取其左右相邻 chunk
2. 合并去重后进行 ReRank 精排
3. 最终取 topK=10（可配置）送入生成器 context
4. 确保跨文档查询时，每个文档至少有 chunk 进入最终 context

详见 `docs/problems.md` P0 问题记录。

本文件不把 Q05、Q10 的测试集标注问题标记为系统缺陷。
