---
id: TEST-20260817-104
type: test-requirement
status: pending-verification
priority: P0
created: 2026-08-17
updated: 2026-08-17
owner: unassigned
version: 1
tags: [space, deletion, async-task, consistency, testing]
---

# Space 子树隔离与异步删除测试要求

## 必选范围

1. 空/非空目录预览统计、确认名称、令牌过期/重放/篡改和并发子树变化。
2. OWNER/ADMIN、MEMBER 全本人/混合子树、VIEWER 权限。
3. 确认事务后列表、搜索、预览、下载、版本、RAG、引用立即拒绝。
4. 重复投递、Worker 崩溃、租约过期、迟到结果、应用重启和管理员重试。
5. Qdrant、MinIO、引用计数、配额和物理清理故障注入。
6. 100 层、宽子树和大量文件性能；不得 Java 深递归。

## 通过标准与记录

- 无撤销入口、无隔离后泄露、无重复释放引用、无共享物理对象误删，最终状态可解释。
- 实现 Agent 创建独立执行记录并回链 TASK-20260817-004，包含故障注入和恢复证据。

## 执行记录（2026-08-17）

- `SpaceFileRemovalLifecycleTest` 与后端全量回归通过；统一任务取消入口明确拒绝 `SPACE_FILE_DELETE`。
- 静态核对通过：确认快照过期、整树隔离、ACTIVE 文件/RAG 查询过滤、逐节点事务、重复处理已删除节点 no-op。
- 未执行：RabbitMQ 重投、Worker 崩溃、Qdrant/MinIO/引用故障注入、100 层/宽树性能和最终对账；Docker 引擎未运行。完成这些门禁并引入 `CONVERGED` 状态后方可标记 `passed`。
