---
id: TEST-20260725-014
type: test-record
status: completed
priority: P0
created: 2026-07-27
updated: 2026-07-27
owner: codex
version: 1
tags: [mq-v3, memory, profile, rabbitmq, qdrant, privacy]
---

# TASK-018 用户记忆任务迁移测试记录

## 覆盖范围

- 记忆抽取、画像派生、向量索引与向量删除的统一 memory 任务链。
- ID-only payload、稳定幂等键、来源/资源版本栅栏、重复事件、取消、模型失败、Qdrant 失败与错误摘要脱敏。
- RabbitMQ 停机补发、memory Feature Flag 回切、V43→V44 升级和真实 model-service/Qdrant 写入。

## 自动测试与构建

```powershell
mvn -pl cloud-server -am test '-Dtest=UserMemoryMqMigrationTest,UserMemoryExtractionServiceTest,UserMemoryServiceTest,UserMemoryManagementServiceTest,UserMemoryRetrievalServiceTest,UnifiedTaskCenterServiceTest,RabbitTaskTopologyTest,MapperSqlContractTest' '-Dsurefire.failIfNoSpecifiedTests=false'
mvn test
mvn -pl cloud-server -am package -DskipTests
Set-Location cloud-frontend
npm run test
npm run build
```

- 专项及直接关联测试：31 个，0 失败、0 错误。
- 后端全量：262 个，0 失败、0 错误。
- 前端全量：12 个，0 失败；TypeScript/Vite 生产构建成功。
- Spring Boot 生产包构建成功，`git diff --check` 通过。

## 自动断言

| 场景 | 结果 |
|---|---|
| 重复业务事件 | 稳定 task key/候选唯一键复用，不生成第二个有效画像 |
| 任务消息和资源键 | payload 仅含领域 ID，规范化键仅以 SHA-256 进入资源键 |
| 模型返回后收到取消 | 不调用 `acceptFromTask`，不持久化画像 |
| 来源在模型调用期间更新 | 抽取任务 `SKIPPED_STALE`，不写候选 |
| 父抽取任务取消 | Profile 不扇出 Vector，候选不能激活 |
| Vector 调用后收到取消 | 激活 CAS 不执行，条目转为 `CANCELED` 并推进版本 |
| 旧资源版本 | Qdrant 完全不调用，任务 stale |
| 模型异常包含正文/Token | 任务与领域错误摘要仅保存固定安全文案 |
| 完成与取消竞争 | `markSuccess` 失败后立即以同一租约收敛 `CANCELED` |

## Win10 Docker 集成验收

- `docker compose config --quiet` 及带测试镜像仓库变量的 Hub Compose 配置均通过。
- MySQL、RabbitMQ、MinIO、Qdrant、model-service 均为 `healthy`；应用以 Win10 用户进程运行，`/actuator/health` 返回 `UP`。
- `ylcloud_task017_acceptance` 从 V43 成功应用 V44 `user memory async tasks`，Flyway 共校验 45 个迁移。
- 正常样本产生任务 73 `MEMORY_PROFILE_BUILD` 和任务 74 `MEMORY_VECTOR_INDEX`，均为 `SUCCESS`、attempt 1；outbox/inbox 均为 `SENT/PROCESSED`。
- model-service 收到真实 `/embed` 请求并返回 200；Qdrant `ylcloud_user_memory_bge_small_zh_v15` 中按 userId/memoryId 计数为 1。
- 应用运行时 memory 主队列消费者为 2，prefetch 1；任务完成后 ready/unacked 均为 0，memory retry/DLQ 均为 0。

## RabbitMQ 故障与 Flag 回切

1. RabbitMQ 停止后插入第二条候选；扫描器创建任务 75，但任务保持 `PENDING_PUBLISH`、outbox 保持 `PENDING`。
2. Broker 停机期间 outbox 累计 13 次发布尝试，Worker 未领取，未发生 embedding 调用。
3. RabbitMQ 恢复后任务 75 `MEMORY_PROFILE_BUILD` 与任务 84 `MEMORY_VECTOR_INDEX` 均 attempt 1 成功，outbox/inbox 为 `SENT/PROCESSED`，条目为 `ACTIVE/READY`。
4. 以 `async.mq.memory=false` 重启后插入第三条候选；旧路径将其激活为 `ACTIVE/READY`，`profile_async_task_id/vector_async_task_id` 均为空，memory 统一任务新增数为 0。

## 任务量、重试与成本观察

- 正常样本：每条候选 1 个 Profile 任务、1 个 Vector 任务、1 次真实 embedding；Worker 重试率 0。
- Rabbit 故障样本：发布重试 13 次，但 Worker attempt 仍为 1，真实 embedding 仍只有 1 次。
- Profile 为 DB-only 编排阶段；相比旧路径没有增加模型调用。抽取模型 API 暂无 provider request key，租约丢失可能重复计费，但候选唯一键可阻止重复画像落库。
- 本轮为小样本功能验收，未将调度周期包含在生产延迟基线；正式容量数据在 mq-v6 统一观察窗口采集。

## 结论

TASK-018 的用户记忆三阶段迁移、取消与来源版本保护、错误脱敏、RabbitMQ 故障恢复、真实向量写入和 Flag 回切全部通过。
