---
id: TEST-20260725-005
type: test-record
status: completed
priority: P0
created: 2026-07-26
updated: 2026-07-26
owner: codex
version: 1
tags: [space, lifecycle, ownership, mysql, concurrency]
---

# TASK-003 Space 生命周期与所有权测试记录

## 测试目标

验证 PERSONAL 永久私有、TEAM 唯一 OWNER、原子转让、成员退出、高风险解散确认、DISSOLVING 隔离和解散 Outbox。

## 自动测试

```powershell
mvn -pl cloud-server -am '-Dmaven.repo.local=.m2repo' `
  '-Dtest=SpaceLifecycleServiceTest,SpaceMemberLifecycleServiceTest,SpacePermissionServiceTest' `
  '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl cloud-server -am '-Dmaven.repo.local=.m2repo' test
mvn -pl cloud-server -am '-Dmaven.repo.local=.m2repo' -DskipTests package
```

- 专项：10 个测试，0 失败、0 错误。
- 后端全量：182 个测试，0 失败、0 错误。
- 生产包：构建成功。

## Win10 Docker MySQL 与 API E2E

- 隔离数据库：`ylcloud_task003_test`。
- Flyway：空库成功应用 35 个迁移，最终版本 V34。
- 用户：OWNER `user_id=1`、成员 `user_id=2`、独立 OWNER `user_id=3`。

| 场景 | HTTP 结果 | 结论 |
|---|---:|---|
| PERSONAL 邀请成员 | 403 | 通过 |
| PERSONAL 退出 | 403 | 通过 |
| PERSONAL 转让 | 403 | 通过 |
| PERSONAL 旧 DELETE | 403 | 通过 |
| TEAM 添加成员 | 200 | 通过 |
| 有其他成员的 OWNER 未转让退出 | 409 | 通过 |
| OWNER 转让 | 200 | 通过 |
| 转让后的旧 OWNER 退出 | 200 | 通过 |
| 错误 Space 名称确认解散 | 409 | 通过 |
| 正确高风险确认解散 | 200 | 通过 |
| DISSOLVING Space 再读取 | 403 | 已从成员授权边界隔离 |

## 并发与数据不变量

- 对同一 Space 同时发起转让给两个不同成员，结果为 `200/403`。
- 竞态结束后 `spaces.owner_id=2`，有效 OWNER 数为 1。
- 已转让 TEAM、DISSOLVING TEAM 和竞态 TEAM 的有效 OWNER 数均为 1。
- 解散 Space 状态为 `DISSOLVING`，仅有一条 `PENDING` 解散 Outbox。
- 每个测试用户的有效 PERSONAL Space 数均为 1。

## 回滚与残留边界

- V34 只添加状态、约束和 Outbox，不执行知识派生物物理删除。
- 解散清理 Consumer 由后续清理/统一任务中心任务实现；在此之前 `DISSOLVING` Space 保持不可访问和可追踪。
- 迁移失败时 Flyway 阻止应用启动；事务转让或 Outbox 写入失败时保留原 OWNER/ACTIVE 状态。

## 结论

PERSONAL/TEAM 生命周期、唯一 OWNER、行锁并发和高风险解散门禁全部通过，TASK-20260725-003 状态回写为 `completed`。
