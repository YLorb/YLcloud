---
id: TEST-20260725-013
type: test-record
status: completed
priority: P0
created: 2026-07-27
updated: 2026-07-27
owner: codex
version: 1
tags: [mq-v2, cleanup, maintenance, rabbitmq, mysql, minio, qdrant]
---

# TASK-017 清理与维护任务迁移测试记录

## 覆盖范围

- 物理文件、过期/已合并分片、跨存储恢复、RAG 清理和一致性验证的统一任务登记与执行。
- 稳定幂等键、资源版本栅栏、迟到删除防护、重复扫描、开关独立性、旧路径避让和 inbox/outbox 收敛。
- RabbitMQ 停机与恢复、真实 MySQL/MinIO/Qdrant、V42→V43 前滚迁移及 Compose 部署配置。

## 自动测试与构建

```powershell
mvn -pl cloud-server -am test '-Dtest=CleanupMaintenanceMqMigrationTest,PhysicalFileCleanupServiceTest,CrossStoreRecoveryServiceTest,RagIndexConsistencyServiceTest,UnifiedTaskCenterServiceTest,RabbitTaskTopologyTest' '-Dsurefire.failIfNoSpecifiedTests=false'
mvn test
mvn -pl cloud-server -am package -DskipTests
Set-Location cloud-frontend
npm run test
npm run build
```

- 专项及直接关联测试：23 个，0 失败、0 错误。
- 后端全量：252 个，0 失败、0 错误。
- 前端全量：12 个，0 失败；TypeScript/Vite 生产构建成功。
- Spring Boot 生产包构建成功，`git diff --check` 通过。

## Win10 Docker 集成验收

- `docker compose config --quiet` 通过；设置测试用镜像仓库变量后 `docker-compose.hub.yml` 配置校验通过。
- MySQL、RabbitMQ、MinIO、Qdrant 容器均为 `healthy`；隔离数据库 `ylcloud_task017_acceptance` 从空库成功应用 44 个迁移至 V43。
- 物理清理 `task017-missing-physical`：统一任务 `PHYSICAL_FILE_CLEANUP` 为 `SUCCESS`、attempt 1；legacy 清理记录为 `SUCCESS`，outbox 为 `SENT`，inbox 为 `PROCESSED`。
- 跨存储恢复 `task017-cross-recovery`：统一任务 `CROSS_STORE_RECOVERY` 为 `SUCCESS`、attempt 1；领域操作按补偿语义落为 `FAILED`，避免把未完成业务写误报为成功。
- cleanup 与 maintenance 队列消费者各 1；本轮任务结束后 ready/unacked 均为 0。

## RabbitMQ 故障演练

1. 停止 RabbitMQ 后登记物理清理对象 `task017-rabbit-outage`。
2. 扫描后 legacy 记录保持 `PENDING`，统一任务保持 `PENDING_PUBLISH`，outbox 保持 `PENDING` 并累计发布尝试，不执行 MinIO 副作用。
3. 恢复 RabbitMQ 后 outbox 自动续发；最终统一任务 `SUCCESS`、attempt 1，legacy 记录 `SUCCESS`，outbox `SENT`、inbox `PROCESSED`。
4. 演练中 outbox 共经历 7 次发布尝试，证明 Broker 故障不会丢任务或增加领域执行次数。

## 升级与关键断言

| 场景 | 结果 |
|---|---|
| 已有数据库 V42 前滚 | 成功应用 V43，随后 44 个迁移校验通过 |
| 前滚后应用重启 | `/actuator/health` 返回 `UP` |
| 重复扫描同一资源版本 | 唯一 `task_key` 复用原任务 |
| 文件在迟到清理前重新登记 | 版本/引用检查拒绝删除，MinIO 未被调用 |
| cleanup 与 maintenance 独立开关 | 仅开启 maintenance 不改变 cleanup 旧路径 |
| 关闭迁移开关但已有活动任务 | 旧执行路径避让，不双跑 |
| RAG 有效索引对账 | 完成版本栅栏并推进一致性版本 |
| RAG 引用或向量计数异常 | 隔离后进入幂等清理 |
| RabbitMQ 停机与恢复 | `PENDING_PUBLISH` 最终收敛为 `SUCCESS` |

## 结论

TASK-017 的清理与维护队列迁移、幂等与版本栅栏、独立灰度/回滚路径、RabbitMQ 故障恢复和 V43 前滚升级全部通过。
