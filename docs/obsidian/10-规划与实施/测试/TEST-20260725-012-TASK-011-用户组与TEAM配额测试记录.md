---
id: TEST-20260725-012
type: test-record
status: completed
priority: P1
created: 2026-07-26
updated: 2026-07-26
owner: codex
version: 1
tags: [quota, team, ledger, reconciliation, mysql]
---

# TASK-011 用户组与 TEAM 配额测试记录

## 覆盖范围

- 用户组策略：存储、单文件、Space、月 API、月模型 Token、月 Agent 任务和 Agent 并发。
- USER/TEAM 独立账户、全局物理去重计量、归属转移、最后引用释放和账本修复。
- `Asia/Shanghai` 自然月、原子限额扣减、异常并发校正、Web/开放 API 用量查询和管理端策略更新。

## 自动测试与构建

```powershell
mvn --% -pl cloud-server -am test -Dtest=QuotaServiceTest,StorageServiceTest,SpaceLifecycleServiceTest,OpenApiKeyInterceptorTest,KnowledgeChatQueryServiceTest -Dsurefire.failIfNoSpecifiedTests=false
mvn test
mvn -pl cloud-server -am package -DskipTests
Set-Location cloud-frontend
npm test -- --run
npm run build
```

- 专项及直接关联测试：26 个，0 失败、0 错误。
- 后端全量：243 个，0 失败、0 错误。
- 前端全量：12 个，0 失败；TypeScript/Vite 生产构建成功。
- Spring Boot 生产包构建成功。

## Win10 Docker/MySQL 验收

- MySQL 与 RabbitMQ 容器均为 `healthy`；隔离数据库为 `ylcloud_task004_acceptance`。
- Flyway 成功校验 43 个迁移，Schema 最终版本 V42 `group team quota ledger`。
- 迁移后共有 5 个配额账户、2 个活动逻辑引用、1 个物理账本对象，物理计量 4043 字节。
- 将部署所有者所在用户组的月 API 限额临时调整为 2，使用真实 API Key 连续访问 `/api/v1/usage`，状态依次为 `200, 200, 429`；月用量从 0 原子增长到 2，未超卖，周期键为 `2026-07-01`。
- 对账结果为 `SUCCEEDED`、差异 0；访问 1 行账本、并发校正 1 行、孤儿和空账本删除均为 0。
- 验收 Key 已置为 `REVOKED`，用户组月 API 限额已恢复为 10000；仅保留当月 2 次真实调用计量作为验收证据。

## 关键断言

| 场景 | 结果 |
|---|---|
| 相同 hash 被 USER/TEAM 多处引用 | 物理账本仅一条，不重复计费 |
| 账本首次创建 | 当前账户在锁内保守校验存储上限 |
| 非最后引用解除 | 引用数减少，归属转移至剩余活动账户 |
| 最后引用解除 | 物理账本删除，计量字节释放 |
| 月 API/Token 消耗 | 单条带上限谓词的 SQL 原子更新 |
| Agent 月量与并发预留 | 同一 SQL 同时校验并增长，不超卖 |
| 进程异常未释放并发 | 30 秒内按真实队列状态校正 |
| 账本漂移 | 定时或管理端手动对账并保留证据 |

## 结论

TASK-011 的分组策略、TEAM 独立配额、自然月用量、全局物理去重、原子扣减、归属释放、对账修复与外部查询全部通过。
