server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${YLCLOUD_DATASOURCE_URL:jdbc:mysql://127.0.0.1:3306/ylcloud?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai}
    username: ${YLCLOUD_DATASOURCE_USERNAME:ylcloud}
    password: ${YLCLOUD_DATASOURCE_PASSWORD:ylcloud}

ylcloud:
  upload:
    max-file-size: 2147483648
  minio:
    endpoint: ${YLCLOUD_MINIO_ENDPOINT:http://127.0.0.1:9000}
    accessKey: ${YLCLOUD_MINIO_ACCESS_KEY:minioadmin}
    secretKey: ${YLCLOUD_MINIO_SECRET_KEY:minioadmin}
    bucket-name: ${YLCLOUD_MINIO_BUCKET:localbucket1}
