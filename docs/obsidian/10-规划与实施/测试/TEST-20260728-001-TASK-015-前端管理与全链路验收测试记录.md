---
id: TEST-20260728-001
type: test-record
status: passed
task: TASK-20260725-015
created: 2026-07-28
owner: Codex
tags: [frontend, contract, e2e, release-gate]
---
# TASK-015 前端管理与全链路验收测试记录

## 验收范围

- 账号状态、注销风险提示、数据导出与一次性解密凭据。
- 安全审计查询、真实字段展示和保留策略。
- 备份归档、READY 状态及隔离恢复验证。
- 维护模式状态、原因、时间与高风险操作确认。
- 桌面和 390×900 移动端布局、生产构建及前后端回归。

## 本轮修复

- 前端类型和请求参数改为与 Java Controller/VO 的真实契约一致，不再把审计列表误当成分页对象。
- 备份页使用 `id/archivePath/archiveHash/archiveSizeBytes`，并查询最新隔离恢复验证。
- 维护页使用 `active/startedAt`，启用原因通过查询参数提交。
- 数据导出在下载前单独请求详情，只在确认对话框中显示短期解密凭据。
- 修复 `Button asChild` 同时渲染占位子节点导致 Radix Slot 页面崩溃的问题。
- 审计保留策略局部更新保留未提交的 `permanent` 和 `description` 字段。

## 自动化结果

| 门禁 | 结果 |
| --- | --- |
| Maven 全量测试 | 306/306 通过 |
| Vitest 全量测试 | 17/17 通过 |
| TASK-015 页面契约测试 | 4/4 通过 |
| TypeScript + Vite 生产构建 | 通过 |
| `git diff --check` | 通过 |

## 真实浏览器证据

使用 Win10 身份运行的 Docker 服务和当前前端镜像，通过 Edge/Playwright CLI 完成：

1. 注册并登录隔离验收账号。
2. 桌面端打开账号设置，账号状态、导出和注销作用域均正确。
3. 打开导出确认对话框并提交真实导出任务；任务 `#2` 出现在历史列表，浏览器控制台 0 error、0 warning。
4. 切换 390×900 视口，账号卡片、导出历史和移动端“更多”导航均可访问。
5. ADMIN 页面因不修改现有部署所有者凭据，使用 Vitest 中与后端真实响应一致的契约组件测试覆盖。

## 发布说明与已知限制

- 本轮属于代码候选验收，不代表已执行生产数据恢复或生产版本升级。
- `deploy.sh` 的成功、应用失败、前端失败、烟测失败、回滚失败、信号中断和恢复门禁共 7 个 Linux 脚本场景已通过；真实生产发布仍必须使用当次 READY 备份完成隔离恢复后执行。
- 浏览器验收创建了隔离测试用户和一条数据导出任务；未修改部署所有者凭据或权限。

## 结论

TASK-015 的前端代码、API 契约、组件测试、生产构建和普通用户浏览器主链路通过。生产上线仍须按 TASK-013/014 门禁提供当次真实备份、隔离恢复和部署日志。
